
/*
Neil Opena
110878452
CSE 114 - Homework #3
*/

import java.util.*;

public class RunLengthEncoding {

	public static void main(String[] args) {
		Scanner myScanner = new Scanner(System.in);
		System.out.print("Enter a string to endode:");
		String message = myScanner.next();

		System.out.print("Enter the delimeter");
		char delimeter = myScanner.next().charAt(0);

		System.out.print(encode(message, delimeter));
	}

	public static String encode(String message, char delimeter) {
		String encoded = "";
		
		String test = "" + message.charAt(0);
		
		for(int i = 0; i < message.length() - 1; i++){
			if(message.charAt(i) != message.charAt(i + 1))
				test = test + message.charAt(i + 1);
		}
		if(message.charAt(message.length() - 2) != message.charAt(message.length() - 1) && test.charAt(test.length() - 1) != message.charAt(message.length() - 1))
			test = test + message.charAt(message.length() - 1);
		
		
		String[] letters = new String[test.length()];
	
		System.out.println(test);
		System.out.println(test.length());

		for(int i = 0; i < letters.length - 3; i++){
			letters[i] = message.substring(message.charAt(test.indexOf(i)), message.charAt(test.indexOf(i + 1)));
		}
		
		return encoded;

	}

}
